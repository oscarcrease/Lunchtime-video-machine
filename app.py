"""
YouTube Picker — main Streamlit entry point.

This is a placeholder for now (step 1: scaffolding). It will be built out
in step 9 to orchestrate the full flow:
  time picker -> creator picker -> video menu -> watch/log

Run with: streamlit run app.py
"""

import streamlit as st

st.set_page_config(page_title="YouTube Picker", page_icon="\U0001F4FA", layout="wide")

st.title("YouTube Picker")
st.info(
    "Scaffolding is up! Nothing to see yet — core logic and UI screens "
    "get built out in the following steps."
)
